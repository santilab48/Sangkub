'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../../../lib/supabase'
export default function Devices(){const [rows,setRows]=useState<any[]>([]),[err,setErr]=useState('');async function load(){const {data,error}=await supabase.from('devices').select('id,name,role,is_active,last_seen_at').order('name');if(error)setErr(error.message);else setRows(data||[])}useEffect(()=>{load()},[]);return <main className="admin"><header><div><b>จุดรับงาน</b><small>ครัว บาร์ แคชเชียร์ และจอ</small></div><a href="/admin">‹ หลังบ้าน</a></header><section className="adminHero"><h1>{rows.filter(x=>x.is_active).length} เครื่องเปิดใช้งาน</h1><p>แต่ละเครื่องรับงานตามหน้าที่ของตัวเอง</p></section>{err&&<div className="empty">{err}</div>}<section className="adminGrid">{rows.map(x=><div className="loginCard" key={x.id}><h2>{x.name}</h2><p>{x.role}</p><small>{x.is_active?'● เปิดใช้งาน':'○ ปิดใช้งาน'}</small></div>)}</section>{!err&&!rows.length&&<div className="empty">ยังไม่มีจุดรับงาน</div>}</main>}
