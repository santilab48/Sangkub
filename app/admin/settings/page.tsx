'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../../../lib/supabase'
export default function Settings(){const [row,setRow]=useState<any>(null),[err,setErr]=useState('');async function load(){const {data,error}=await supabase.from('restaurants').select('id,name,plan,daily_order_limit,device_slots,currency,promptpay_id,is_active').limit(1).maybeSingle();if(error)setErr(error.message);else setRow(data)}useEffect(()=>{load()},[]);return <main className="admin"><header><div><b>ตั้งค่าร้าน</b><small>แพ็กเกจและการรับชำระ</small></div><a href="/admin">‹ หลังบ้าน</a></header>{err&&<div className="empty">{err}</div>}{row?<section className="loginCard"><h1>{row.name}</h1><p>แพ็กเกจ <b>{String(row.plan).toUpperCase()}</b></p><p>จุดรับงาน <b>{row.device_slots} เครื่อง</b></p><p>โควตาออเดอร์/วัน <b>{row.daily_order_limit}</b></p><p>PromptPay <b>{row.promptpay_id?'ตั้งค่าแล้ว':'ยังไม่ได้ตั้งค่า'}</b></p><small>{row.is_active?'ร้านเปิดใช้งาน':'ร้านถูกปิดใช้งาน'}</small></section>:!err&&<div className="empty">กำลังโหลด…</div>}</main>}
